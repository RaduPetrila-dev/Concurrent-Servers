# Build output
build/
*.o
*.d
*.a
*.so
*.out
a.out

# Binaries built in place by mistake
sequential_server
threaded_server
threadpool_server
epoll_server
select_server
uv_server
blocking_listener
nonblocking_listener
threadspammer
loadgen

# Benchmark raw latency dumps. Regenerable from bench/run_bench.sh, and large:
# one line per request, so a 1024-connection sweep runs to tens of megabytes.
# summary.csv, environment.txt and the plots are committed.
results/*/raw/

# Debug and profiling
core
core.*
vgcore.*
perf.data
perf.data.old
callgrind.out.*
massif.out.*
*.gcda
*.gcno
*.gcov

# Tooling
compile_commands.json
.cache/
.clangd/

# Python (benchmark plotting)
__pycache__/
*.py[cod]
.venv/
venv/
.ipynb_checkpoints/

# Editors
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db
